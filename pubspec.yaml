name: bot_2000
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #Providers
  provider: ^6.0.1
  get_it: ^7.2.0

  #http
  dio: ^4.0.4

  #navigation
  auto_route: ^3.2.0

  #UI
  google_fonts: ^2.1.0

  #localDb
  hive: ^2.0.5
  hive_flutter: ^1.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^3.2.0
  hive_generator: ^1.1.1
  build_runner: ^2.1.7

  flutter_lints: ^1.0.0


flutter:
  assets:
      - assets/fake_db/user.json



 
  uses-material-design: true
